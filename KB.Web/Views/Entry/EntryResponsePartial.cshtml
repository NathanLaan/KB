@model KB.Web.Models.EntryResponseModel

@using (Html.BeginForm("EntryResponsePartial","Entry")) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Reply</legend>
        @Html.HiddenFor(m => m.Entry.ParentID)
        @Html.TextAreaFor(m => m.Entry.Contents, 4, 200, new { @class = "input-xxlarge" })
        @Html.ValidationMessageFor(m => m.Entry.Contents)
        <p>
            <input type="submit" value="Reply" />
        </p>
    </fieldset>
}